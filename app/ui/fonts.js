import { Lato, Risque } from "next/font/google";

export const lato = Lato({ 
  weight: ["400", "700"], 
  subsets: ["latin"],
});
export const risque = Risque({
  weight: ["400"],
  subsets: ["latin"],
});
